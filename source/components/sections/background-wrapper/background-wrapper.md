Create Component: background-wrapper: includes, component, scss, acf

just a div.background-wrapper

In acf add a color field (white default)
Add select of padding top and bottom - use sizes from source/styles/library/_size-variables.scss as options (from 1 to 6-5). Add the padding as inline style with var(--size-x)

In Acf this component contains INNER BLOCKS